apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: frontend
  description: SPA frontend do CloudLab (React + nginx)
  tags:
    - react
    - nginx
    - spa
  links:
    - url: https://cloudlab.example.com
      title: Aplicação (produção)
      icon: web
spec:
  type: website
  lifecycle: production
  owner: team-cloudlab
  system: cloudlab
  consumesApis:
    - cloudlab-api
